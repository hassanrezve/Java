import java.util.Random;

public class Topic3 {
    public static void main(String[] args) {
        Random r=new Random();
//        int x=r.nextInt(100);   //it will start from 0 to 99 if we want from 1 to 100 than wirte +1 in front of it
//       double x=r.nextDouble();
        int x=10;
        int y=20;
        int z=Math.max(x,y);
        System.out.println(z);
    }
}
